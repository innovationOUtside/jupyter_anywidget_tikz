{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.js --minify --format=esm --bundle --outdir=src/jupyter_anywidget_tikz/static --loader:.html=text"
	},
	"dependencies": {
		"node-tikzjax": "^1.0.3",
		"tikzjax": "^1.0.2"
	},
	"devDependencies": {
		"esbuild": "^0.23.0"
	}
}
